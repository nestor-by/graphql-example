dependencies {
  compile(deps.reactor.core)
  compile(deps.apollo.runtime)
  compile(deps.apollo.core)

  compile(deps.okhttp.core)
  compile(deps.okhttp.webSockets)
  compile(deps.okhttp.debug.loggingInterceptor)
}