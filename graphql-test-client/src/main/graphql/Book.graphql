query finBooksCount($id: [Long]!){
  finBooksCount(ids: $id) {
    book {
      id
      language {
        description
      }
      author {
        lastName
      }
      title
    }
    stockSum
  }
}

